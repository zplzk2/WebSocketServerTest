# WebSocketServerTest

A test solution for WebSocket.
  * `DateTimeBroadcaster` works as a datetime broadcaster, written in CSharp.
  * `GenericClientGo` works as a client and message receiver, written in GoLang.
